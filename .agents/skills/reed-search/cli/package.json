{
  "name": "reed-cli",
  "version": "1.0.0",
  "description": "CLI for searching reed.co.uk job listings — no authentication, zero runtime dependencies. Search results are parsed from data-qa hooks; job pages from schema.org JobPosting JSON-LD.",
  "type": "module",
  "main": "src/cli.ts",
  "bin": {
    "reed-search": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test --timeout 30000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/bun": "1.3.14"
  }
}
